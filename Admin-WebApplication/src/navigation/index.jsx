import main from './main'
import device from './device'
import history from './history'
import user from './user'

const navigation = [...main, ...history, ...device, ...user]

export default navigation
